export enum Direction {
  Left = 'left',
  Right = 'right',
}

export enum Color {
  LightBlue = 'lightBlue',
  LightGreen = 'lightGreen',
}

export enum ScrollMode {
  Disable = 'hidden',
  Enable = '',
}
